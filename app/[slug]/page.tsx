// The board's home page is the public leaderboard/display — anyone landing
// on /<slug> should see it immediately, no login required. Raising an OD
// and voting live at /<slug>/raise instead.
export { default } from "./display/page";
